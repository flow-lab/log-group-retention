version: 0.1

phases:
  install:
    commands:
      - go get -u golang.org/x/lint/golint
  pre_build:
    commands:
      - golint -set_exit_status
      - go get -d ./...
      - go test
  build:
    commands:
      - go build -o main

artifacts:
  files:
    - app
    - buildspec.yml
    - codedeploy/*
    - cloudformation/*
